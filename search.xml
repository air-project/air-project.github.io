<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[ArrayBlockingQueue]]></title>
    <url>%2F2018%2F11%2F13%2FArrayBlockingQueue%2F</url>
    <content type="text"><![CDATA[翻译类注释 1234567891011121314151617181920212223242526272829303132333435363738394041/*** A bounded &#123;@linkplain BlockingQueue blocking queue&#125; backed by an* array. This queue orders elements FIFO (first-in-first-out). The* &lt;em&gt;head&lt;/em&gt; of the queue is that element that has been on the* queue the longest time. The &lt;em&gt;tail&lt;/em&gt; of the queue is that* element that has been on the queue the shortest time. New elements* are inserted at the tail of the queue, and the queue retrieval* operations obtain elements at the head of the queue.*一个有限的blocking queue由数组支持。 这个队列排列元素FIFO（先进先出）。 队列的头部是队列中最长时间的元素。 头部和尾部为什么是最长和最短告知一下我队列的尾部是队列中最短时间的元素。 新元素插入队列的尾部，队列检索操作获取队列头部的元素* This is a classic &amp;quot;bounded buffer&amp;quot;, in which a* fixed-sized array holds elements inserted by producers and* extracted by consumers. Once created, the capacity cannot be* changed. Attempts to &#123;@code put&#125; an element into a full queue* will result in the operation blocking; attempts to &#123;@code take&#125; an* element from an empty queue will similarly block.* 这是一个经典的“有界缓冲区”，其中固定大小的数组保存由生产者插入的元素并由消费者提取。 创建后，容量无法更改。 尝试put成满的队列的元件将导致操作阻挡; 尝试take从空队列的元件将同样地阻塞 * This class supports an optional fairness policy for ordering* waiting producer and consumer threads. By default, this ordering* is not guaranteed. However, a queue constructed with fairness set* to &#123;@code true&#125; grants threads access in FIFO order. Fairness* generally decreases throughput but reduces variability and avoids* starvation.* 此类支持可选的公平策略，用于订购等待的生产者和消费者线程。 默认情况下，此订单不能保证。 然而，以公平设置为true的队列以FIFO顺序授予线程访问权限。 公平性通常会降低吞吐量，但会降低变异性并避免饥饿 */ 小结关键词： 阻塞 访问顺序 疑问？java里什么可以实现阻塞：synchronized，lock…访问顺序由简单的true,false控制公平性是不是很容易想到ReentrantLock接下来带着疑问，继续源码 源码构造方法12345678910111213141516171819202122232425public ArrayBlockingQueue(int capacity) &#123; this(capacity, false);&#125;/** * Creates an &#123;@code ArrayBlockingQueue&#125; with the given (fixed) * capacity and the specified access policy. * * @param capacity the capacity of this queue * @param fair if &#123;@code true&#125; then queue accesses for threads blocked * on insertion or removal, are processed in FIFO order; * if &#123;@code false&#125; the access order is unspecified. */public ArrayBlockingQueue(int capacity, boolean fair) &#123; if (capacity &lt;= 0) throw new IllegalArgumentException(); //前面类注释提到 Once created, the capacity cannot be changed //那items这个成员变量修饰符上应该有一个final吧，果然 //final Object[] items; this.items = new Object[capacity]; //果不其然，再这里使用了ReentrantLock，如果这不熟悉，参考ReentrantLock这一节 lock = new ReentrantLock(fair);//fair为true，表示是公平锁；fair为false，表示是非公平锁。 notEmpty = lock.newCondition();//notEmpty和notFull是锁的两个Condition条件 notFull = lock.newCondition();&#125; Lock的作用是提供独占锁机制，来保护竞争资源；而Condition是为了更加精细的对锁进行控制，它依赖于Lock，通过某个条件对多线程进行控制。notEmpty表示“锁的非空条件”。当某线程想从队列中取数据时，而此时又没有数据，则该线程通过notEmpty.await()进行等待；当其它线程向队列中插入了元素之后，就调用notEmpty.signal()唤醒“之前通过notEmpty.await()进入等待状态的线程”。同理，notFull表示“锁的满条件”。当某线程想向队列中插入元素，而此时队列已满时，该线程等待；当其它线程从队列中取出元素之后，就唤醒该等待的线程。 当然还有两个比较关心的方法put,take put12345678910111213141516171819202122232425262728293031public void put(E e) throws InterruptedException &#123; // 创建插入的元素是否为null，是的话抛出NullPointerException异常 checkNotNull(e); // 获取“该阻塞队列的独占锁” final ReentrantLock lock = this.lock; // 获取“锁”，若当前线程是中断状态，则抛出InterruptedException异常 lock.lockInterruptibly(); try &#123; // 若“队列为满”，则一直等待。 while (count == items.length) notFull.await(); enqueue(e);// 如果队列未满，则插入e。 &#125; finally &#123; // 释放“锁” lock.unlock(); &#125;&#125;private void enqueue(E x) &#123; final Object[] items = this.items; // 将x添加到”队列“中 items[putIndex] = x; // 设置”下一个被取出元素的索引“ //若++putIndex的值等于“队列的长度”，即添加元素之后，队列满；则设置“下一个被添加元素的索引”为0。 if (++putIndex == items.length) putIndex = 0; // 将”队列中的元素个数”+1 count++; // 唤醒notEmpty上的等待线程 notEmpty.signal();&#125; take1234567891011121314151617181920212223242526272829303132333435363738public E take() throws InterruptedException &#123; // 获取“该阻塞队列的独占锁” final ReentrantLock lock = this.lock; // 获取“锁”，若当前线程是中断状态，则抛出InterruptedException异常 lock.lockInterruptibly(); try &#123; // 若“队列为空”，则一直等待。 while (count == 0) notEmpty.await(); return dequeue();// 取出元素 &#125; finally &#123; // 释放“锁” lock.unlock(); &#125;&#125;private E dequeue() &#123; final Object[] items = this.items; // 强制将元素转换为“泛型E” @SuppressWarnings(&quot;unchecked&quot;) E x = (E) items[takeIndex]; // 将第takeIndex元素设为null，即删除。同时，帮助GC回收。 items[takeIndex] = null; // 设置“下一个被取出元素的索引” //若++takeIndex的值等于“队列的长度”，即取出元素之后，队列空；则设置“下一个被取出元素的索引”为0。 if (++takeIndex == items.length) takeIndex = 0; // 将“队列中元素数量”-1 count--; if (itrs != null) itrs.elementDequeued();//同时更新迭代器中的元素数据，这个什么时候需要呢？ // 唤醒notFull上的等待线程。 notFull.signal(); return x;&#125; 重要的成员变量123456789101112131415161718192021222324252627/** The queued items */final Object[] items;/** items index for next take, poll, peek or remove */int takeIndex;// 下一个被取出元素的索引/** items index for next put, offer, or add */int putIndex;// 下一个被添加元素的索引/** Number of elements in the queue *///队列中元素的个数int count;/* * Concurrency control uses the classic two-condition algorithm * found in any textbook. *//** Main lock guarding all access *///主锁保护所有通道final ReentrantLock lock;/** Condition for waiting takes */private final Condition notEmpty;/** Condition for waiting puts */private final Condition notFull; 至此，我们从类注释上该了解的内容就是这些了。。 总结 ArrayBlockingQueue是一个基于数组的阻塞并发队列，并且在初始化的时候必须指定整个容器的大小（也就是成员变量数组的大小），并且后面也会知道，整个容器是不会扩容的，并且默认使用的是非公平锁。 Condition的signal()，await()需要使用在lock,unlock之间才有效。]]></content>
      <categories>
        <category>源码</category>
      </categories>
      <tags>
        <tag>jkd8</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[BlockingQueue翻译]]></title>
    <url>%2F2018%2F11%2F11%2FBlockingQueue%E7%BF%BB%E8%AF%91%2F</url>
    <content type="text"><![CDATA[翻译类注释 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768697071727374757677787980818283/** * A &#123;@link java.util.Queue&#125; that additionally supports operations * that wait for the queue to become non-empty when retrieving an * element, and wait for space to become available in the queue when * storing an element. * A Queue额外支持在检索元素时等待队列变为非空和 在存储元素时等待队列中的空间变得可用的操作 * &#123;@code BlockingQueue&#125; methods come in four forms, with different ways * of handling operations that cannot be satisfied immediately, but may be * satisfied at some point in the future: BlockingQueue方法有四种形式，具有不同的操作方式，不能立即满足，但可能在将来的某个时间点满足 * one throws an exception, the second returns a special value (either * &#123;@code null&#125; or &#123;@code false&#125;, depending on the operation), the third * blocks the current thread indefinitely until the operation can succeed, * and the fourth blocks for only a given maximum time limit before giving * up. 一个抛出异常，第二个返回一个特殊值（ null或false ，具体取决于操作），第三个程序将无限期地阻止当前线程，直到操作成功为止， 而第四个程序块在放弃之前只有给定的最大时限。 * * A &#123;@code BlockingQueue&#125; does not accept &#123;@code null&#125; elements. * Implementations throw &#123;@code NullPointerException&#125; on attempts * to &#123;@code add&#125;, &#123;@code put&#125; or &#123;@code offer&#125; a &#123;@code null&#125;. A * &#123;@code null&#125; is used as a sentinel value to indicate failure of * &#123;@code poll&#125; operations. A BlockingQueue不接受null元素。 实现抛出NullPointerException上尝试add put或offer一个null 。 A null用作哨兵值以指示poll操作失败。 * * A &#123;@code BlockingQueue&#125; may be capacity bounded. At any given * time it may have a &#123;@code remainingCapacity&#125; beyond which no * additional elements can be &#123;@code put&#125; without blocking. * A &#123;@code BlockingQueue&#125; without any intrinsic capacity constraints always * reports a remaining capacity of &#123;@code Integer.MAX_VALUE&#125;. * A BlockingQueue可能是容量有限的。 在任何给定的时间它可能有一个remainingCapacity超过其中没有额外的元素可以put没有阻止。 没有任何内在容量限制的A BlockingQueue总是报告剩余容量为Integer.MAX_VALUE * &#123;@code BlockingQueue&#125; implementations are designed to be used * primarily for producer-consumer queues, but additionally support * the &#123;@link java.util.Collection&#125; interface. So, for example, it is * possible to remove an arbitrary element from a queue using * &#123;@code remove(x)&#125;. However, such operations are in general * &lt;em&gt;not&lt;/em&gt; performed very efficiently, and are intended for only * occasional use, such as when a queued message is cancelled. * BlockingQueue实现被设计为主要用于生产者 - 消费者队列，但另外支持Collection接口。 因此，例如，可以使用remove(x)从队列中删除任意元素。 然而，这样的操作通常不能非常有效地执行，并且仅用于偶尔使用，例如当排队的消息被取消时。 * &#123;@code BlockingQueue&#125; implementations are thread-safe. All * queuing methods achieve their effects atomically using internal * locks or other forms of concurrency control. However, the * &lt;em&gt;bulk&lt;/em&gt; Collection operations &#123;@code addAll&#125;, * &#123;@code containsAll&#125;, &#123;@code retainAll&#125; and &#123;@code removeAll&#125; are * &lt;em&gt;not&lt;/em&gt; necessarily performed atomically unless specified * otherwise in an implementation. So it is possible, for example, for * &#123;@code addAll(c)&#125; to fail (throwing an exception) after adding * only some of the elements in &#123;@code c&#125;. * BlockingQueue实现是线程安全的。 所有排队方法使用内部锁或其他形式的并发控制在原子上实现其效果。 然而， 大量的Collection操作addAll ， containsAll ， retainAll和removeAll 不一定原子除非在实现中另有规定执行。 因此有可能，例如，为addAll(c)到只增加一些元件在后失败（抛出异常） c 。 * A &#123;@code BlockingQueue&#125; does &lt;em&gt;not&lt;/em&gt; intrinsically support * any kind of &amp;quot;close&amp;quot; or &amp;quot;shutdown&amp;quot; operation to * indicate that no more items will be added. The needs and usage of * such features tend to be implementation-dependent. For example, a * common tactic is for producers to insert special * &lt;em&gt;end-of-stream&lt;/em&gt; or &lt;em&gt;poison&lt;/em&gt; objects, that are * interpreted accordingly when taken by consumers. * A BlockingQueue上不支持任何类型的“关闭”或“关闭”操作，表示不再添加项目。 这些功能的需求和使用往往依赖于实现。 例如，一个常见的策略是生产者插入特殊的尾流或毒物 ，这些消费者在被消费者摄取时被相应地解释 * * Usage example, based on a typical producer-consumer scenario. * Note that a &#123;@code BlockingQueue&#125; can safely be used with multiple * producers and multiple consumers. 使用示例，基于典型的生产者 - 消费者场景。 请注意， BlockingQueue可以安全地与多个生产者和多个消费者一起使用。 示例12345678910111213141516171819202122232425262728293031323334* class Producer implements Runnable &#123;* private final BlockingQueue queue;* Producer(BlockingQueue q) &#123; queue = q; &#125;* public void run() &#123;* try &#123;* while (true) &#123; queue.put(produce()); &#125;* &#125; catch (InterruptedException ex) &#123; ... handle ...&#125;* &#125;* Object produce() &#123; ... &#125;* &#125;** class Consumer implements Runnable &#123;* private final BlockingQueue queue;* Consumer(BlockingQueue q) &#123; queue = q; &#125;* public void run() &#123;* try &#123;* while (true) &#123; consume(queue.take()); &#125;* &#125; catch (InterruptedException ex) &#123; ... handle ...&#125;* &#125;* void consume(Object x) &#123; ... &#125;* &#125;** class Setup &#123;* void main() &#123;* BlockingQueue q = new SomeQueueImplementation();* Producer p = new Producer(q);* Consumer c1 = new Consumer(q);* Consumer c2 = new Consumer(q);* new Thread(p).start();* new Thread(c1).start();* new Thread(c2).start();* &#125;* &#125;&#125; * 1234567891011* Memory consistency effects: As with other concurrent* collections, actions in a thread prior to placing an object into a* &#123;@code BlockingQueue&#125;* &lt;a href=&quot;package-summary.html#MemoryVisibility&quot;&gt;&lt;i&gt;happen-before&lt;/i&gt;&lt;/a&gt;* actions subsequent to the access or removal of that element from* the &#123;@code BlockingQueue&#125; in another thread.** 存储器一致性效果：当与其他并发集合，事先将物体放置成在一个线程动作BlockingQueue happen-before到该元素的从访问或移除后续动作BlockingQueue在另一个线程。 源码 interface BlockingQueue&lt;E&gt; extends Queue&lt;E&gt; &#123; /** * Inserts the specified element into this queue if it is possible to do * so immediately without violating capacity restrictions, returning * &#123;@code true&#125; upon success and throwing an * &#123;@code IllegalStateException&#125; if no space is currently available. * When using a capacity-restricted queue, it is generally preferable to * use &#123;@link #offer(Object) offer&#125;. * * 将指定的元素插入此队列中，如果它是立即可行且不会违反容量限制，返回true在成功和抛出IllegalStateException如果当前没有空间可用。 当使用容量限制队列时，通常最好使用offer 。 */ boolean add(E e); /** * Inserts the specified element into this queue if it is possible to do * so immediately without violating capacity restrictions, returning * &#123;@code true&#125; upon success and &#123;@code false&#125; if no space is currently * available. When using a capacity-restricted queue, this method is * generally preferable to &#123;@link #add&#125;, which can fail to insert an * element only by throwing an exception. * * 将指定的元素插入此队列中，如果它是立即可行且不会违反容量限制，返回true在成功和false ，如果当前没有空间可用。 当使用容量限制队列时，此方法通常优于add(E) ，这可能无法仅通过抛出异常来插入元素。 */ boolean offer(E e); /** * Inserts the specified element into this queue, waiting if necessary * for space to become available. * * 将指定的元素插入到此队列中，等待空间可用。 */ void put(E e) throws InterruptedException; /** * Inserts the specified element into this queue, waiting up to the * specified wait time if necessary for space to become available. * * 将指定的元素插入到此队列中，等待指定的等待时间（如有必要）才能使空间变得可用。 */ boolean offer(E e, long timeout, TimeUnit unit) throws InterruptedException; /** * Retrieves and removes the head of this queue, waiting if necessary * until an element becomes available. * * 检索并删除此队列的头，如有必要，等待元素可用。 */ E take() throws InterruptedException; /** * Retrieves and removes the head of this queue, waiting up to the * specified wait time if necessary for an element to become available. * * 检索并删除此队列的头，等待指定的等待时间（如有必要）使元素变为可用。 */ E poll(long timeout, TimeUnit unit) throws InterruptedException; /** * Returns the number of additional elements that this queue can ideally * (in the absence of memory or resource constraints) accept without * blocking, or &#123;@code Integer.MAX_VALUE&#125; if there is no intrinsic * limit. * 返回该队列最好可以（在没有存储器或资源约束）接受而不会阻塞，或附加的元素的数量Integer.MAX_VALUE如果没有固有的限制。 * &lt;p&gt;Note that you &lt;em&gt;cannot&lt;/em&gt; always tell if an attempt to insert * an element will succeed by inspecting &#123;@code remainingCapacity&#125; * because it may be the case that another thread is about to * insert or remove an element. * * 请注意，您不能总是通过检查remainingCapacity来判断是否尝试插入元素，因为可能是另一个线程即将插入或删除元素的情况 */ int remainingCapacity(); /** * Removes a single instance of the specified element from this queue, * if it is present. More formally, removes an element &#123;@code e&#125; such * that &#123;@code o.equals(e)&#125;, if this queue contains one or more such * elements. * Returns &#123;@code true&#125; if this queue contained the specified element * (or equivalently, if this queue changed as a result of the call). * * 从该队列中删除指定元素的单个实例（如果存在）。 更正式地，删除一个元素e ，使得o.equals(e) ，如果这个队列包含一个或多个这样的元素。 如果此队列包含指定的元素（或等效地，如果此队列作为调用的结果而更改），则返回true 。 */ boolean remove(Object o); /** * Returns &#123;@code true&#125; if this queue contains the specified element. * More formally, returns &#123;@code true&#125; if and only if this queue contains * at least one element &#123;@code e&#125; such that &#123;@code o.equals(e)&#125;. * * 如果此队列包含指定的元素，则返回true 。 更正式地，返回true如果且仅当这个队列至少包含一个元素e ，使得o.equals(e) 。 */ public boolean contains(Object o); /** * Removes all available elements from this queue and adds them * to the given collection. This operation may be more * efficient than repeatedly polling this queue. A failure * encountered while attempting to add elements to * collection &#123;@code c&#125; may result in elements being in neither, * either or both collections when the associated exception is * thrown. Attempts to drain a queue to itself result in * &#123;@code IllegalArgumentException&#125;. Further, the behavior of * this operation is undefined if the specified collection is * modified while the operation is in progress. * * 从该队列中删除所有可用的元素，并将它们添加到给定的集合中。 此操作可能比重复轮询此队列更有效。 尝试向集合c添加元素时遇到的c可能会导致在抛出关联的异常时， 元素既不在两个集合中，也可能不是两个集合。 尝试将队列排入自身会导致IllegalArgumentException 。 此外，如果在操作进行中修改了指定的集合，则此操作的行为是未定义的。 */ int drainTo(Collection&lt;? super E&gt; c); /** * Removes at most the given number of available elements from * this queue and adds them to the given collection. A failure * encountered while attempting to add elements to * collection &#123;@code c&#125; may result in elements being in neither, * either or both collections when the associated exception is * thrown. Attempts to drain a queue to itself result in * &#123;@code IllegalArgumentException&#125;. Further, the behavior of * this operation is undefined if the specified collection is * modified while the operation is in progress. * * 最多从该队列中删除给定数量的可用元素，并将它们添加到给定的集合中。 尝试向集合c添加元素时遇到的c可能会导致在抛出关联的异常时， 元素既不在两个集合中，也可能不是两个集合。 尝试将队列排入自身导致IllegalArgumentException 。 此外，如果在操作进行中修改了指定的集合，则此操作的行为是未定义的。 */ int drainTo(Collection&lt;? super E&gt; c, int maxElements);&#125; 总结 是一种线程安全的队列 一般用于这样的场景：一个线程生产对象，另一个线程来消耗对象 不能向BlockingQueue中插入null,否则会抛出NullPointerException异常 operation Throws Exception Special Value Blocks新 Times Out新 Insert add(o) offer(o) put(o) 阻塞 offer(o, timeout, timeunit) Remove remove(o) poll() take() 阻塞 poll(timeout, timeunit) Examine element() peek()]]></content>
      <categories>
        <category>源码</category>
      </categories>
      <tags>
        <tag>jkd8</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Queue翻译]]></title>
    <url>%2F2018%2F11%2F11%2FQueue%E7%BF%BB%E8%AF%91%2F</url>
    <content type="text"><![CDATA[翻译类注释 123456789101112131415161718192021222324252627A collection designed for holding elements prior to processing.一个设计用来保存需要预先处理元素的CollectionBesides basic &#123;@link java.util.Collection Collection&#125; operations,queues provide additional insertion, extraction, and inspectionoperations. 除了基本的 Collection操作,队列提供额外的插入、提取和检验操作Each of these methods exists in two forms: one throwsan exception if the operation fails, the other returns a specialvalue (either &#123;@code null&#125; or &#123;@code false&#125;, depending on theoperation). 这些方法中存在两种形式:一个抛出一个异常,如果操作失败, 另一个返回一个特殊的值( null或者 false,根据操作)The latter form of the insert operation is designedspecifically for use with capacity-restricted &#123;@code Queue&#125;implementations; 后者的插入操作形式是专门为使用容量限制 Queue实现;in most implementations, insert operations cannot fail.在大多数实现,insert操作不能失败。 六个主要方法 method Throws exception Returns special value Insert add(e) offer(e) Remove remove() poll() Examine element() peek() 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081Queues typically, but do not necessarily, order elements in aFIFO (first-in-first-out) manner. 队列通常,但不一定,顺序元素FIFO(先进先出)的方式Among the exceptions arepriority queues, which order elements according to a suppliedcomparator, or the elements&apos; natural ordering, and LIFO queues (orstacks) which order the elements LIFO (last-in-first-out).例如在优先队列中,顺序元素根据提供的比较器,或者元素的自然顺序,后进先出队列顺序(或堆栈)后进先出的元素。 Whatever the ordering used, the &lt;em&gt;head&lt;/em&gt; of the queue is thatelement which would be removed by a call to &#123;@link #remove() &#125; or&#123;@link #poll()&#125;. In a FIFO queue, all new elements are inserted atthe &lt;em&gt;tail&lt;/em&gt; of the queue. Other kinds of queues may usedifferent placement rules. Every &#123;@code Queue&#125; implementationmust specify its ordering properties.无论使用哪种顺序,队列的头部是元素将被调用remove()或poll()。在FIFO队列,所有新元素插入到队列的尾部。其他类型的队列可能使用不同的放置规则。每个Queue实现必须指定其排序属性。The &#123;@link #offer offer&#125; method inserts an element if possible,otherwise returning &#123;@code false&#125;. This differs from the &#123;@linkjava.util.Collection#add Collection.add&#125; method, which can fail toadd an element only by throwing an unchecked exception. The&#123;@code offer&#125; method is designed for use when failure is a normal,rather than exceptional occurrence, for example, in fixed-capacity(or &amp;quot;bounded&amp;quot;) queues.offer方法插入一个元素如果可能,否则返回false。这不同于Collection.add方法,它可以不添加一个元素只有抛出未检测的异常。offer方法是设计用于当失败是正常的,而不是特殊情况,例如,在固定电容(或“有界”)队列。The &#123;@link #remove()&#125; and &#123;@link #poll()&#125; methods remove andreturn the head of the queue.Exactly which element is removed from the queue is afunction of the queue&apos;s ordering policy, which differs fromimplementation to implementation. The &#123;@code remove()&#125; and&#123;@code poll()&#125; methods differ only in their behavior when thequeue is empty: the &#123;@code remove()&#125; method throws an exception,while the &#123;@code poll()&#125; method returns &#123;@code null&#125;. remove()和poll()方法删除并返回队列的头部。从队列中删除哪些元素是一个函数队列的排序策略,这不同于实现来实现。remove()和poll()方法不同队列为空时:remove()方法抛出一个异常,而poll()方法返回null。The &#123;@link #element()&#125; and &#123;@link #peek()&#125; methods return, but donot remove, the head of the queue.element() peek()方法返回,但不要删除队列的头部。The &#123;@code Queue&#125; interface does not define the &lt;i&gt;blocking queuemethods&lt;/i&gt;, which are common in concurrent programming. These methods,which wait for elements to appear or for space to become available, aredefined in the &#123;@link java.util.concurrent.BlockingQueue&#125; interface, whichextends this interface.Queue接口没有定义阻塞队列的方法,常见的并发编程。这些方法,等待元素出现或空间可用BlockingQueue接口中定义,扩展这个接口。&#123;@code Queue&#125; implementations generally do not allow insertionof &#123;@code null&#125; elements, although some implementations, such as&#123;@link LinkedList&#125;, do not prohibit insertion of &#123;@code null&#125;.Even in the implementations that permit it, &#123;@code null&#125; shouldnot be inserted into a &#123;@code Queue&#125;, as &#123;@code null&#125; is alsoused as a special return value by the &#123;@code poll&#125; method toindicate that the queue contains no elements.Queue null元素的实现通常不允许插入,尽管一些实现,如LinkedList、不禁止插入null。即使在允许它的实现,null不应插入一个Queue null也用作特殊poll返回值的方法表明,队列中不包含任何元素。&#123;@code Queue&#125; implementations generally do not defineelement-based versions of methods &#123;@code equals&#125; and&#123;@code hashCode&#125; but instead inherit the identity based versionsfrom class &#123;@code Object&#125;, because element-based equality is notalways well-defined for queues with the same elements but differentordering properties.Queue实现通常不定义的元素版本方法equals hashCode而是继承类Object的基于身份的版本,因为元素相同的平等并不总是明确的队列元素但不同排序属性。 源码 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152public interface Queue&lt;E&gt; extends Collection&lt;E&gt; &#123; /** * Inserts the specified element into this queue if it is possible to do so * immediately without violating capacity restrictions, returning * &#123;@code true&#125; upon success and throwing an &#123;@code IllegalStateException&#125; * if no space is currently available. * 将指定的元素插入此队列能否立即这样做,在不违反容量限制,返回 true成功 和抛出 IllegalStateException如果没有目前可用的空间 */ boolean add(E e); /** * Inserts the specified element into this queue if it is possible to do * so immediately without violating capacity restrictions. * When using a capacity-restricted queue, this method is generally * preferable to &#123;@link #add&#125;, which can fail to insert an element only * by throwing an exception. * 将指定的元素插入此队列能否立即这样做,在不违反容量限制。 使用capacity-restricted队列时,这种方法通常比 add(E),无法插入一个元素只有通过抛出异常。 */ boolean offer(E e); /** * Retrieves and removes the head of this queue. This method differs * from &#123;@link #poll poll&#125; only in that it throws an exception if this * queue is empty. * 检索并删除此队列的头。这个方法与 poll唯一的不同之处在于,它将抛出一个异常,如果这个队列是空的。 */ E remove(); /** * Retrieves and removes the head of this queue, * or returns &#123;@code null&#125; if this queue is empty. * 检索并删除此队列的头,或者返回 null如果这个队列是空的。 */ E poll(); /** * Retrieves, but does not remove, the head of this queue. This method * differs from &#123;@link #peek peek&#125; only in that it throws an exception * if this queue is empty. * 检索,但不删除此队列的头。这个方法与 peek唯一的不同之处在于,它将抛出一个异常,如果这个队列是空的。 */ E element(); /** * Retrieves, but does not remove, the head of this queue, * or returns &#123;@code null&#125; if this queue is empty. * 检索,但不删除此队列的头,或者返回 null如果这个队列是空的。 */ E peek();&#125; 总结 队列是什么？就是用来装一组元素的集合 队列并不都是FIFO 队列并不提供阻塞（由子类实现) 队列可以指定容量 一类抛异常一类为不抛]]></content>
      <categories>
        <category>源码</category>
      </categories>
      <tags>
        <tag>jkd8</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[深度解析jdk8-AbstractQueuedSynchronizer的实现分析下]]></title>
    <url>%2F2018%2F09%2F24%2F%E6%B7%B1%E5%BA%A6%E8%A7%A3%E6%9E%90jdk8-AbstractQueuedSynchronizer%E7%9A%84%E5%AE%9E%E7%8E%B0%E5%88%86%E6%9E%90%E4%B8%8B%2F</url>
    <content type="text"><![CDATA[前言经过本系列的上半部分JDK1.8 AbstractQueuedSynchronizer的实现分析（上）的解读，相信很多读者已经对AbstractQueuedSynchronizer(下文简称AQS)的独占功能了然于胸,那么这次我们通过对另一个工具类:CountDownLatch的分析来解读AQS的另外一个功能：共享功能。 AQS共享功能的实现在开始解读AQS的共享功能前，我们再重温一下CountDownLatch，CountDownLatch为java.util.concurrent包下的计数器工具类，常被用在多线程环境下，它在初始时需要指定一个计数器的大小，然后可被多个线程并发的实现减1操作，并在计数器为0后调用await方法的线程被唤醒，从而实现多线程间的协作。它在多线程环境下的基本使用方式为： 12345678910111213141516171819202122232425 //main thread // 新建一个CountDownLatch，并指制定一个初始大小 CountDownLatch countDownLatch = new CountDownLatch(3); // 调用await方法后，main线程将阻塞在这里，直到countDownLatch 中的计数为0 countDownLatch.await(); System.out.println(&quot;over&quot;);//thread1// do something //...........//调用countDown方法，将计数减1 countDownLatch.countDown();//thread2// do something //...........//调用countDown方法，将计数减1 countDownLatch.countDown(); //thread3// do something //...........//调用countDown方法，将计数减1 countDownLatch.countDown(); 注意，线程thread 1,2,3各自调用 countDown后，countDownLatch 的计数为0，await方法返回，控制台输入“over”,在此之前main thread 会一直沉睡。 可以看到CountDownLatch的作用类似于一个“栏栅”，在CountDownLatch的计数为0前，调用await方法的线程将一直阻塞，直到CountDownLatch计数为0，await方法才会返回， 而CountDownLatch的countDown()方法则一般由各个线程调用，实现CountDownLatch计数的减1。 知道了CountDownLatch的基本使用方式，我们就从上述DEMO的第一行new CountDownLatch（3）开始，看看CountDownLatch是怎么实现的。 首先，看下CountDownLatch的构造方法：1234public CountDownLatch(int count) &#123; if (count &lt; 0) throw new IllegalArgumentException(&quot;count &lt; 0&quot;); this.sync = new Sync(count);&#125; 和ReentrantLock类似，CountDownLatch内部也有一个叫做Sync的内部类，同样也是用它继承了AQS。 再看下Sync：123Sync(int count) &#123; setState(count);&#125; 如果你看过本系列的上半部分，你对setState方法一定不会陌生，它是AQS的一个“状态位”，在不同的场景下，代表不同的含义，比如在ReentrantLock中，表示加锁的次数，在CountDownLatch中，则表示CountDownLatch的计数器的初始大小。设置完计数器大小后CountDownLatch的构造方法返回，下面我们再看下CountDownLatch的await()方法：123public void await() throws InterruptedException &#123; sync.acquireSharedInterruptibly(1);&#125; 调用了Sync的acquireSharedInterruptibly方法，因为Sync是AQS子类的原因，这里其实是直接调用了AQS的acquireSharedInterruptibly方法：1234567public final void acquireSharedInterruptibly(int arg) throws InterruptedException &#123; if (Thread.interrupted()) throw new InterruptedException(); if (tryAcquireShared(arg) &lt; 0) doAcquireSharedInterruptibly(arg);&#125; 从方法名上看，这个方法的调用是响应线程的打断的，所以在前两行会检查下线程是否被打断。接着，尝试着获取共享锁，小于0，表示获取失败，通过本系列的上半部分的解读， 我们知道AQS在获取锁的思路是，先尝试直接获取锁，如果失败会将当前线程放在队列中，按照FIFO的原则等待锁。而对于共享锁也是这个思路，如果和独占锁一致，这里的tryAcquireShared应该是个空方法，留给子类去判断:123protected int tryAcquireShared(int arg) &#123; throw new UnsupportedOperationException();&#125; 再看看CountDownLatch：123protected int tryAcquireShared(int acquires) &#123; return (getState() == 0) ? 1 : -1;&#125; 如果state变成0了，则返回1，表示获取成功，否则返回-1则表示获取失败。 看到这里，读者可能会发现， await方法的获取方式更像是在获取一个独占锁，那为什么这里还会用tryAcquireShared呢？ 回想下CountDownLatch的await方法是不是只能在主线程中调用？答案是否定的，CountDownLatch的await方法可以在多个线程中调用，当CountDownLatch的计数器为0后，调用await的方法都会依次返回。 也就是说可以多个线程同时在等待await方法返回，所以它被设计成了实现tryAcquireShared方法，获取的是一个共享锁，锁在所有调用await方法的线程间共享，所以叫共享锁。 回到acquireSharedInterruptibly方法：1234567public final void acquireSharedInterruptibly(int arg) throws InterruptedException &#123; if (Thread.interrupted()) throw new InterruptedException(); if (tryAcquireShared(arg) &lt; 0) doAcquireSharedInterruptibly(arg);&#125; 如果获取共享锁失败（返回了-1，说明state不为0，也就是CountDownLatch的计数器还不为0），进入调用doAcquireSharedInterruptibly方法中，按照我们上述的猜想，应该是要将当前线程放入到队列中去。 在这之前，我们再回顾一下AQS队列的数据结构：AQS是一个双向链表，通过节点中的next，pre变量分别指向当前节点后一个节点和前一个节点。其中，每个节点中都包含了一个线程和一个类型变量：表示当前节点是独占节点还是共享节点，头节点中的线程为正在占有锁的线程，而后的所有节点的线程表示为正在等待获取锁的线程。如下图所示：黄色节点为头节点，表示正在获取锁的节点，剩下的蓝色节点（Node1、Node2、Node3）为正在等待锁的节点，他们通过各自的next、pre变量分别指向前后节点，形成了AQS中的双向链表。每个线程被加上类型（共享还是独占）后便是一个Node， 也就是本文中说的节点。 再看看doAcquireSharedInterruptibly方法：1234567891011121314151617181920212223242526272829private void doAcquireSharedInterruptibly(int arg) throws InterruptedException &#123; final Node node = addWaiter(Node.SHARED); //将当前线程包装为类型为Node.SHARED的节点，标示这是一个共享节点。 boolean failed = true; try &#123; for (;;) &#123; final Node p = node.predecessor(); if (p == head) &#123;//如果新建节点的前一个节点，就是Head，说明当前节点是AQS队列中等待获取锁的第一个节点，//按照FIFO的原则，可以直接尝试获取锁。 int r = tryAcquireShared(arg); if (r &gt;= 0) &#123; setHeadAndPropagate(node, r); //获取成功，需要将当前节点设置为AQS队列中的第一个节点，这是AQS的规则//队列的头节点表示正在获取锁的节点 p.next = null; // help GC failed = false; return; &#125; &#125; if (shouldParkAfterFailedAcquire(p, node) &amp;&amp; //检查下是否需要将当前节点挂起 parkAndCheckInterrupt()) throw new InterruptedException(); &#125; &#125; finally &#123; if (failed) cancelAcquire(node); &#125; &#125; 这里有几点需要说明的： setHeadAndPropagate方法：12345678910private void setHeadAndPropagate(Node node, int propagate) &#123; Node h = head; // Record old head for check below setHead(node); if (propagate &gt; 0 || h == null || h.waitStatus &lt; 0 || (h = head) == null || h.waitStatus &lt; 0) &#123; Node s = node.next; if (s == null || s.isShared()) doReleaseShared(); &#125;&#125; 首先，使用了CAS更换了头节点，然后，将当前节点的下一个节点取出来，如果同样是“shared”类型的，再做一个”releaseShared”操作。 看下doReleaseShared方法：1234567891011121314151617181920for (;;) &#123; Node h = head; if (h != null &amp;&amp; h != tail) &#123; int ws = h.waitStatus; if (ws == Node.SIGNAL) &#123; if (!compareAndSetWaitStatus(h, Node.SIGNAL, 0)) //如果当前节点是SIGNAL意味着，它正在等待一个信号， //或者说，它在等待被唤醒，因此做两件事，1是重置waitStatus标志位，2是重置成功后,唤醒下一个节点。 continue; // loop to recheck cases unparkSuccessor(h); &#125; else if (ws == 0 &amp;&amp; !compareAndSetWaitStatus(h, 0, Node.PROPAGATE)) //如果本身头节点的waitStatus是出于重置状态（waitStatus==0）的，将其设置为“传播”状态。//意味着需要将状态向后一个节点传播。 continue; // loop on failed CAS &#125; if (h == head) // loop if head changed break; &#125; 为什么要这么做呢？这就是共享功能和独占功能最不一样的地方，对于独占功能来说，有且只有一个线程（通常只对应一个节点，拿ReentantLock举例，如果当前持有锁的线程重复调用lock()方法，那根据本系列上半部分我们的介绍，我们知道，会被包装成多个节点在AQS的队列中，所以用一个线程来描述更准确），能够获取锁，但是对于共享功能来说。 共享的状态是可以被共享的，也就是意味着其他AQS队列中的其他节点也应能第一时间知道状态的变化。因此，一个节点获取到共享状态流程图是这样的：比如现在有如下队列：当Node1调用tryAcquireShared成功后，更换了头节点： Node1变成了头节点然后调用unparkSuccessor()方法唤醒了Node2、Node2中持有的线程A出于上面流程图的park node的位置， 线程A被唤醒后，重复黄色线条的流程，重新检查调用tryAcquireShared方法，看能否成功，如果成功，则又更改头节点，重复以上步骤，以实现节点自身获取共享锁成功后，唤醒下一个共享类型节点的操作，实现共享状态的向后传递。 2.其实对于doAcquireShared方法，AQS还提供了集中类似的实现：分别对应了： 带参数请求共享锁。 （忽略中断） 带参数请求共享锁，且响应中断。（每次循环时，会检查当前线程的中断状态，以实现对线程中断的响应） 带参数请求共享锁但是限制等待时间。（第二个参数设置超时时间，超出时间后，方法返回。）比较特别的为最后一个doAcquireSharedNanos方法，我们一起看下它怎么实现超时时间的控制的。 因为该方法和其余获取共享锁的方法逻辑是类似的，我用红色框圈出了它所不一样的地方，也就是实现超时时间控制的地方。 可以看到，其实就是在进入方法时，计算出了一个“deadline”，每次循环的时候用当前时间和“deadline”比较，大于“dealine”说明超时时间已到，直接返回方法。 注意，最后一个红框中的这行代码：1nanosTimeout &gt; spinForTimeoutThreshold 从变量的字面意思可知，这是拿超时时间和超时自旋的最小作比较，在这里Doug Lea把超时自旋的阈值设置成了1000ns,即只有超时时间大于1000ns才会去挂起线程，否则，再次循环，以实现“自旋”操作。这是“自旋”在AQS中的应用之处。 看完await方法，我们再来看下countDown()方法：123public void countDown() &#123; sync.releaseShared(1);&#125; 调用了AQS的releaseShared方法,并传入了参数1:1234567public final boolean releaseShared(int arg) &#123; if (tryReleaseShared(arg)) &#123; doReleaseShared(); return true; &#125; return false;&#125; 同样先尝试去释放锁，tryReleaseShared同样为空方法，留给子类自己去实现，以下是CountDownLatch的内部类Sync的实现：1234567891011protected boolean tryReleaseShared(int releases) &#123; // Decrement count; signal when transition to zero for (;;) &#123; int c = getState(); if (c == 0) return false; int nextc = c-1; if (compareAndSetState(c, nextc)) return nextc == 0; &#125; &#125; 死循环更新state的值，实现state的减1操作，之所以用死循环是为了确保state值的更新成功。 从上文的分析中可知，如果state的值为0，在CountDownLatch中意味：所有的子线程已经执行完毕，这个时候可以唤醒调用await()方法的线程了，而这些线程正在AQS的队列中，并被挂起的， 所以下一步应该去唤醒AQS队列中的头节点了（AQS的队列为FIFO队列），然后由头节点去依次唤醒AQS队列中的其他共享节点。 如果tryReleaseShared返回true,进入doReleaseShared()方法：12345678910111213141516171819202122private void doReleaseShared() &#123; for (;;) &#123; Node h = head; if (h != null &amp;&amp; h != tail) &#123; int ws = h.waitStatus; if (ws == Node.SIGNAL) &#123; if (!compareAndSetWaitStatus(h, Node.SIGNAL, 0)) //如果当前节点是SIGNAL意味着，它正在等待一个信号， //或者说，它在等待被唤醒，因此做两件事，1是重置waitStatus标志位，2是重置成功后,唤醒下一个节点。 continue; // loop to recheck cases unparkSuccessor(h); &#125; else if (ws == 0 &amp;&amp; !compareAndSetWaitStatus(h, 0, Node.PROPAGATE)) //如果本身头节点的waitStatus是出于重置状态（waitStatus==0）的，将其设置为“传播”状态。//意味着需要将状态向后一个节点传播。 continue; // loop on failed CAS &#125; if (h == head) // loop if head changed break; &#125; &#125; 当线程被唤醒后，会重新尝试获取共享锁，而对于CountDownLatch线程获取共享锁判断依据是state是否为0，而这个时候显然state已经变成了0，因此可以顺利获取共享锁并且依次唤醒AQS队里中后面的节点及对应的线程。 总结本文从CountDownLatch入手，深入分析了AQS关于共享锁方面的实现方式： 如果获取共享锁失败后，将请求共享锁的线程封装成Node对象放入AQS的队列中，并挂起Node对象对应的线程，实现请求锁线程的等待操作。待共享锁可以被获取后，从头节点开始，依次唤醒头节点及其以后的所有共享类型的节点。实现共享状态的传播。 这里有几点值得注意：与AQS的独占功能一样，共享锁是否可以被获取的判断为空方法，交由子类去实现。与AQS的独占功能不同，当锁被头节点获取后，独占功能是只有头节点获取锁，其余节点的线程继续沉睡，等待锁被释放后，才会唤醒下一个节点的线程，而共享功能是只要头节点获取锁成功，就在唤醒自身节点对应的线程的同时，继续唤醒AQS队列中的下一个节点的线程，每个节点在唤醒自身的同时还会唤醒下一个节点对应的线程，以实现共享状态的“向后传播”，从而实现共享功能。以上的分析都是从AQS子类的角度去看待AQS的部分功能的，而如果直接看待AQS，或许可以这么去解读： 首先，AQS并不关心“是什么锁”，对于AQS来说它只是实现了一系列的用于判断“资源”是否可以访问的API,并且封装了在“访问资源”受限时将请求访问的线程的加入队列、挂起、唤醒等操作， AQS只关心“资源不可以访问时，怎么处理？”、“资源是可以被同时访问，还是在同一时间只能被一个线程访问？”、“如果有线程等不及资源了，怎么从AQS的队列中退出？”等一系列围绕资源访问的问题，而至于“资源是否可以被访问？”这个问题则交给AQS的子类去实现。 当AQS的子类是实现独占功能时，例如ReentrantLock，“资源是否可以被访问”被定义为只要AQS的state变量不为0，并且持有锁的线程不是当前线程，则代表资源不能访问。 当AQS的子类是实现共享功能时，例如：CountDownLatch，“资源是否可以被访问”被定义为只要AQS的state变量不为0，说明资源不能访问。 这是典型的将规则和操作分开的设计思路：规则子类定义，操作逻辑因为具有公用性，放在父类中去封装。 当然，正式因为AQS只是关心“资源在什么条件下可被访问”，所以子类还可以同时使用AQS的共享功能和独占功能的API以实现更为复杂的功能。 比如：ReentrantReadWriteLock，我们知道ReentrantReadWriteLock的中也有一个叫Sync的内部类继承了AQS，而AQS的队列可以同时存放共享锁和独占锁，对于ReentrantReadWriteLock来说分别代表读锁和写锁，当队列中的头节点为读锁时，代表读操作可以执行，而写操作不能执行，因此请求写操作的线程会被挂起，当读操作依次推出后，写锁成为头节点，请求写操作的线程被唤醒，可以执行写操作，而此时的读请求将被封装成Node放入AQS的队列中。如此往复，实现读写锁的读写交替进行。 而本系列文章上半部分提到的FutureTask，其实思路也是：封装一个存放线程执行结果的变量A,使用AQS的独占API实现线程对变量A的独占访问，判断规则是，线程没有执行完毕：call()方法没有返回前，不能访问变量A，或者是超时时间没到前不能访问变量A(这就是FutureTask的get方法可以实现获取线程执行结果时，设置超时时间的原因)。原网页地址：http://www.infoq.com/cn/articles/java8-abstractqueuedsynchronizer#]]></content>
  </entry>
  <entry>
    <title><![CDATA[深度解析jdk8-AbstractQueuedSynchronizer的实现分析上]]></title>
    <url>%2F2018%2F09%2F24%2F%E6%B7%B1%E5%BA%A6%E8%A7%A3%E6%9E%90jdk8-AbstractQueuedSynchronizer%E7%9A%84%E5%AE%9E%E7%8E%B0%E5%88%86%E6%9E%90%E4%B8%8A%2F</url>
    <content type="text"><![CDATA[前言Java中的FutureTask作为可异步执行任务并可获取执行结果而被大家所熟知。通常可以使用future.get()来获取线程的执行结果，在线程执行结束之前，get方法会一直阻塞状态，直到call()返回，其优点是使用线程异步执行任务的情况下还可以获取到线程的执行结果，但是FutureTask的以上功能却是依靠通过一个叫AbstractQueuedSynchronizer的类来实现，至少在JDK1.5、JDK1.6版本是这样的（从1.7开始FutureTask已经被其作者Doug Lea修改为不再依赖AbstractQueuedSynchronizer实现了，这是JDK1.7的变化之一）。但是AbstractQueuedSynchronizer在JDK1.8中还有如下图所示的众多子类：这些JDK中的工具类或多或少都被大家用过不止一次，比如ReentrantLock，我们知道ReentrantLock的功能是实现代码段的并发访问控制，也就是通常意义上所说的锁，在没有看到AbstractQueuedSynchronizer前，可能会以为它的实现是通过类似于synchronized，通过对对象加锁来实现的。但事实上它仅仅是一个工具类！没有使用更“高级”的机器指令，不是关键字，也不依靠JDK编译时的特殊处理，仅仅作为一个普普通通的类就完成了代码块的并发访问控制，这就更让人疑问它怎么实现的代码块的并发访问控制的了。那就让我们一起来仔细看下Doug Lea怎么去实现的这个锁。为了方便，本文中使用AQS代替AbstractQueuedSynchronizer。 细说AQS在深入分析AQS之前，我想先从AQS的功能上说明下AQS，站在使用者的角度，AQS的功能可以分为两类：独占功能和共享功能，它的所有子类中，要么实现并使用了它独占功能的API，要么使用了共享锁的功能，而不会同时使用两套API，即便是它最有名的子类ReentrantReadWriteLock，也是通过两个内部类：读锁和写锁，分别实现的两套API来实现的，为什么这么做，后面我们再分析，到目前为止，我们只需要明白AQS在功能上有独占控制和共享控制两种功能即可。 独占锁在真正对解读AQS之前，我想先从使用了它独占控制功能的子类ReentrantLock说起，分析ReentrantLock的同时看一看AQS的实现，再推理出AQS独特的设计思路和实现方式。最后，再看其共享控制功能的实现。对于ReentrantLock，使用过的同学应该都知道，通常是这么用它的：123reentrantLock.lock()//do somethingreentrantLock.unlock() ReentrantLock会保证 do something在同一时间只有一个线程在执行这段代码，或者说，同一时刻只有一个线程的lock方法会返回。其余线程会被挂起，直到获取锁。从这里可以看出，其实ReentrantLock实现的就是一个独占锁的功能：有且只有一个线程获取到锁，其余线程全部挂起，直到该拥有锁的线程释放锁，被挂起的线程被唤醒重新开始竞争锁。没错，ReentrantLock使用的就是AQS的独占API实现的。那现在我们就从ReentrantLock的实现开始一起看看重入锁是怎么实现的。首先看lock方法： 123public void lock() &#123; sync.lock();&#125; 如FutureTask（JDK1.6）一样，ReentrantLock内部有代理类完成具体操作，ReentrantLock只是封装了统一的一套API而已。值得注意的是，使用过ReentrantLock的同学应该知道，ReentrantLock又分为公平锁和非公平锁，所以，ReentrantLock内部只有两个sync的实现：公平锁：每个线程抢占锁的顺序为先后调用lock方法的顺序依次获取锁，类似于排队吃饭。非公平锁：每个线程抢占锁的顺序不定，谁运气好，谁就获取到锁，和调用lock方法的先后顺序无关，类似于堵车时，加塞的那些XXXX。到这里，通过ReentrantLock的功能和锁的所谓排不排队的方式，我们是否可以这么猜测ReentrantLock或者AQS的实现（现在不清楚谁去实现这些功能）：有那么一个被volatile修饰的标志位叫做key，用来表示有没有线程拿走了锁，或者说，锁还存不存在，还需要一个线程安全的队列，维护一堆被挂起的线程，以至于当锁被归还时，能通知到这些被挂起的线程，可以来竞争获取锁了。至于公平锁和非公平锁，唯一的区别是在获取锁的时候是直接去获取锁，还是进入队列排队的问题了。为了验证我们的猜想，我们继续看一下ReentrantLock中公平锁的实现：123final void lock() &#123; acquire(1); &#125; 调用到了AQS的acquire方法：12345public final void acquire(int arg) &#123; //tryAcquire 由子类实现，模板方法模式 if (!tryAcquire(arg) &amp;&amp; //如果没有获取到，则addWaiter加入等待队列，并挂起线程 acquireQueued(addWaiter(Node.EXCLUSIVE), arg)) selfInterrupt(); //挂起后唤醒返回的中断状态是true的话，这里会中断当前线程&#125; 从方法名字上看语义是，尝试获取锁，获取不到则创建一个waiter（当前线程）后放到队列中，这和我们猜测的好像很类似。[G1]先看下tryAcquire方法:123protected boolean tryAcquire(int arg) &#123; throw new UnsupportedOperationException();&#125; 留空了，Doug Lea是想留给子类去实现（既然要给子类实现，应该用抽象方法，但是Doug Lea没有这么做，原因是AQS有两种功能，面向两种使用场景，需要给子类定义的方法都是抽象方法了，会导致子类无论如何都需要实现另外一种场景的抽象方法，显然，这对子类来说是不友好的。）看下FairSync的tryAcquire方法：getState方法是AQS的方法，因为在AQS里面有个叫statede的标志位 :事实上，这个state就是前面我们猜想的那个“key”！回到tryAcquire方法： 123456789101112131415161718192021222324protected final boolean tryAcquire(int acquires) &#123; final Thread current = Thread.currentThread();//获取当前线程 int c = getState(); //获取父类AQS中的标志位 if (c == 0) &#123; if (!hasQueuedPredecessors() &amp;&amp; //如果队列中没有其他线程 说明没有线程正在占有锁！ compareAndSetState(0, acquires)) &#123; //修改一下状态位，注意：这里的acquires是在lock的时候传递来的，从上面的图中可以知道，这个值是写死的1 setExclusiveOwnerThread(current); //如果通过CAS操作将状态为更新成功则代表当前线程获取锁，因此，将当前线程设置到AQS的一个变量中，说明这个线程拿走了锁。 return true; &#125; &#125; else if (current == getExclusiveOwnerThread()) &#123; //如果不为0 意味着，锁已经被拿走了，但是，因为ReentrantLock是重入锁， //是可以重复lock,unlock的，只要成对出现行。一次。这里还要再判断一次 获取锁的线程是不是当前请求锁的线程。 int nextc = c + acquires;//如果是的，累加在state字段上就可以了。 if (nextc &lt; 0) throw new Error(&quot;Maximum lock count exceeded&quot;); setState(nextc); return true; &#125; return false;&#125; 到此，如果如果获取锁，tryAcquire返回true，反之，返回false，回到AQS的acquire方法。如果没有获取到锁，按照我们的描述，应该将当前线程放到队列中去，只不过，在放之前，需要做些包装。先看addWaiter方法：12345678910111213141516171819private Node addWaiter(Node mode) &#123; //初始化一个node节点 Node node = new Node(Thread.currentThread(), mode); //先尝试直接加入到尾节点后面 // Try the fast path of enq; backup to full enq on failure Node pred = tail; if (pred != null) &#123; node.prev = pred;//从这里也可以看出，先将node的pre指向尾节点，然后cas设置tail，再将原tail的next指向新节点， //所以可能next为空的情况存在,但是已经加入的节点的pre肯定是存在 if (compareAndSetTail(pred, node)) &#123; pred.next = node; return node; &#125; &#125; //失败的话，for循环loop加入 enq(node); return node;&#125; 用当前线程去构造一个Node对象，mode是一个表示Node类型的字段，仅仅表示这个节点是独占的，还是共享的，或者说，AQS的这个队列中，哪些节点是独占的，哪些是共享的。这里lock调用的是AQS独占的API，当然，可以写死是独占状态的节点。创建好节点后，将节点加入到队列尾部，此处，在队列不为空的时候，先尝试通过cas方式修改尾节点为最新的节点，如果修改失败，意味着有并发，这个时候才会进入enq中死循环，“自旋”方式修改。将线程的节点接入到队里中后，当然还需要做一件事:将当前线程挂起！这个事，由acquireQueued来做。在解释acquireQueued之前，我们需要先看下AQS中队列的内存结构，我们知道，队列由Node类型的节点组成，其中至少有两个变量，一个封装线程，一个封装节点类型。而实际上，它的内存结构是这样的（第一次节点插入时，第一个节点是一个空节点，代表有一个线程已经获取锁，事实上，队列的第一个节点就是代表持有锁的节点）：黄色节点为队列默认的头节点，每次有线程竞争失败，进入队列后其实都是插入到队列的尾节点（tail后面）后面。这个从enq方法可以看出来，上文中有提到enq方法为将节点插入队列的方法:123456789101112131415161718private Node enq(final Node node) &#123; //loop操作，tail不存在的情况会初始化一个空节点，并将head和tail都指向空节点， //然后cas加入node，确保节点一定会加入 for (;;) &#123; Node t = tail; if (t == null) &#123; // Must initialize if (compareAndSetHead(new Node())) tail = head; &#125; else &#123; node.prev = t; if (compareAndSetTail(t, node)) &#123; t.next = node; return t; &#125; &#125; &#125; &#125; 再回来看看 123456789101112131415161718192021222324final boolean acquireQueued(final Node node, int arg) &#123; boolean failed = true; try &#123; boolean interrupted = false; for (;;) &#123; final Node p = node.predecessor(); if (p == head &amp;&amp; tryAcquire(arg)) &#123; //如果当前的节点是head说明他是队列中第一个“有效的”节点，因此尝试获取，上文中有提到这个类是交给子类去扩展的。 setHead(node);//成功后，将上图中的黄色节点移除，Node1变成头节点。 p.next = null; // help GC failed = false; return interrupted; &#125; if (shouldParkAfterFailedAcquire(p, node) &amp;&amp; //否则，检查前一个节点的状态为，看当前获取锁失败的线程是否需要挂起。 parkAndCheckInterrupt()) //如果需要，借助JUC包下的LockSopport类的静态方法Park挂起当前线程。知道被唤醒。 interrupted = true; &#125; &#125; finally &#123; if (failed) //如果有异常 cancelAcquire(node);// 取消请求，对应到队列操作，就是将当前节点从队列中移除。 &#125;&#125; 这块代码有几点需要说明： Node节点中，除了存储当前线程，节点类型，队列中前后元素的变量，还有一个叫waitStatus的变量，改变量用于描述节点的状态，为什么需要这个状态呢？原因是：AQS的队列中，在有并发时，肯定会存取一定数量的节点，每个节点[G4] 代表了一个线程的状态，有的线程可能“等不及”获取锁了，需要放弃竞争，退出队列，有的线程在等待一些条件满足，满足后才恢复执行（这里的描述很像某个J.U.C包下的工具类，ReentrankLock的Condition，事实上，Condition同样也是AQS的子类）等等，总之，各个线程有各个线程的状态，但总需要一个变量来描述它，这个变量就叫waitStatus,它有四种状态：分别表示： 节点取消 节点等待触发 节点等待条件 节点状态需要向后传播。 只有当前节点的前一个节点为SIGNAL时，才能当前节点才能被挂起。 对线程的挂起及唤醒操作是通过使用UNSAFE类调用JNI方法实现的。当然，还提供了挂起指定时间后唤醒的API，在后面我们会讲到。到此为止，一个线程对于锁的一次竞争才告于段落，结果有两种，要么成功获取到锁（不用进入到AQS队列中），要么，获取失败，被挂起，等待下次唤醒后继续循环尝试获取锁，值得注意的是，AQS的队列为FIFO队列，所以，每次被CPU假唤醒，且当前线程不是出在头节点的位置，也是会被挂起的。AQS通过这样的方式，实现了竞争的排队策略。看完了获取锁，在看看释放锁，具体看代码之前，我们可以先继续猜下，释放操作需要做哪些事情：因为获取锁的线程的节点，此时在AQS的头节点位置，所以，可能需要将头节点移除。而应该是直接释放锁，然后找到AQS的头节点，通知它可以来竞争锁了。是不是这样呢?我们继续来看下，同样我们用ReentrantLock的FairSync来说明： 12345678910111213public void unlock() &#123; sync.release(1);&#125;public final boolean release(int arg) &#123; if (tryRelease(arg)) &#123; Node h = head; if (h != null &amp;&amp; h.waitStatus != 0) unparkSuccessor(h); return true; &#125; return false;&#125; unlock方法调用了AQS的release方法，同样传入了参数1，和获取锁的相应对应，获取一个锁，标示为+1，释放一个锁，标志位-1。同样，release为空方法，子类自己实现逻辑：123456789101112protected final boolean tryRelease(int releases) &#123; int c = getState() - releases; if (Thread.currentThread() != getExclusiveOwnerThread()) //如果释放的线程和获取锁的线程不是同一个，抛出非法监视器状态异常。 throw new IllegalMonitorStateException(); boolean free = false; if (c == 0) &#123;//因为是重入的关系，不是每次释放锁c都等于0，直到最后一次释放锁时，才通知AQS不需要再记录哪个线程正在获取锁。 free = true; setExclusiveOwnerThread(null); &#125; setState(c); return free; &#125; 释放锁，成功后，找到AQS的头节点，并唤醒它即可：123456789101112131415161718192021222324252627private void unparkSuccessor(Node node) &#123; /* * If status is negative (i.e., possibly needing signal) try * to clear in anticipation of signalling. It is OK if this * fails or if status is changed by waiting thread. */ // 尝试将node的等待状态置为0,这样的话,后继争用线程可以有机会再尝试获取一次锁。 int ws = node.waitStatus; if (ws &lt; 0) compareAndSetWaitStatus(node, ws, 0); /* * Thread to unpark is held in successor, which is normally * just the next node. But if cancelled or apparently null, * traverse backwards from tail to find the actual * non-cancelled successor. */ Node s = node.next; if (s == null || s.waitStatus &gt; 0) &#123; s = null; for (Node t = tail; t != null &amp;&amp; t != node; t = t.prev) if (t.waitStatus &lt;= 0) s = t; &#125; if (s != null) LockSupport.unpark(s.thread); &#125; 值得注意的是，寻找的顺序是从队列尾部开始往前去找的最前面的一个waitStatus小于0的节点。到此，ReentrantLock的lock和unlock方法已经基本解析完毕了，唯独还剩下一个非公平锁NonfairSync没说，其实，它和公平锁的唯一区别就是获取锁的方式不同，一个是按前后顺序一次获取锁，一个是抢占式的获取锁，那ReentrantLock是怎么实现的呢？再看两段代码：非公平锁的lock方法的处理方式是: 在lock的时候先直接cas修改一次state变量（尝试获取锁），成功就返回，不成功再排队，从而达到不排队直接抢占的目的。123456static final class FairSync extends Sync &#123; private static final long serialVersionUID = -3000897897090466540L; final void lock() &#123; acquire(1); &#125; 而对于公平锁：则是老老实实的开始就走AQS的流程排队获取锁。如果前面有人调用过其lock方法，则排在队列中前面，也就更有机会更早的获取锁，从而达到“公平”的目的。 总结这篇文章，我们从ReentrantLock出发，完整的分析了AQS独占功能的API及内部实现，总的来说，思路其实并不复杂，还是使用的标志位+队列的方式，记录获取锁、竞争锁、释放锁等一系列锁的状态，或许用更准确一点的描述的话，应该是使用的标志位+队列的方式，记录锁、竞争、释放等一系列独占的状态，因为站在AQS的层面state可以表示锁，也可以表示其他状态，它并不关心它的子类把它变成一个什么工具类，而只是提供了一套维护一个独占状态。甚至，最准确的是AQS只是维护了一个状态，因为，别忘了，它还有一套共享状态的API，所以，AQS只是维护一个状态，一个控制各个线程何时可以访问的状态，它只对状态负责，而这个状态表示什么含义，由子类自己去定义。 原网页地址：http://www.infoq.com/cn/articles/jdk1.8-abstractqueuedsynchronizer]]></content>
  </entry>
  <entry>
    <title><![CDATA[Hexo+GitHub搭建笔记]]></title>
    <url>%2F2018%2F09%2F20%2FHexo%2BGitHub%E6%90%AD%E5%BB%BA%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[Node 安装下载对应版本的Node,直接下一步安装就行，然后检查 1234$ node -vv10.10.0$ npm -v6.4.1 Git 安装下载对应版本的Git,直接下一步安装就行，然后检查 12$ git --versiongit version 2.15.1.windows.2 Hexo 安装1234567891011121314151617181920212223242526$ mkdir hexo$ cd hexo$ npm install hexo -g$ hexo -vhexo: 3.7.1hexo-cli: 1.1.0os: Windows_NT 10.0.17134 win32 x64http_parser: 2.8.0node: 10.10.0v8: 6.8.275.30-node.24uv: 1.23.0zlib: 1.2.11ares: 1.14.0modules: 64nghttp2: 1.33.0napi: 3openssl: 1.1.0iicu: 62.1unicode: 11.0cldr: 33.1tz: 2018e $ hexo s //启动，打开浏览器http://localhost:4000就能看到了INFO Start processingINFO Hexo is running at http://localhost:4000 . Press Ctrl+C to stop. Hexo 插件123456789101112部署到Git,订阅,统计,站内搜索等$ npm install hexo-deployer-git --save$ npm install hexo-generator-feed --save$ npm install hexo-wordcount --save$ npm install hexo-generator-search --save$ npm install hexo-asset-image --save$ npm install hexo-generator-index --save$ npm install hexo-generator-archive --save$ npm install hexo-generator-category --save$ npm install hexo-generator-tag --save$ npm install hexo-server --save$ npm install hexo-generator-sitemap Hexo 配置这里只讲几个常用的，更多配置官方 || 主题next安装 hexo/_config.yml如下这些做修改 12345678910language: zh-Hans //修改为中文post_asset_folder: true //图片文件夹生成为XXX.MD &gt;$ hexo n "XXX" 并在其中插入图片![](XXX/test.png)theme: next //主题默认为landscapedeploy: type: git repo: ********/air-project.github.io.git branch: master hexo/themes/next/_config.yml如下这些做修改 1234567891011121314151617181920212223242526272829303132333435363738394041424344rss: /atom.xmlfooter: icon: heart powered: true theme: version: falsemenu: home: / || home tags: /tags/ || tags categories: /categories/ || th archives: /archives/ || archivemenu_icons: enable: true categories: th tags: tags archives: archivepost_wordcount: item_text: true wordcount: true min2read: true totalcount: true separated_meta: true busuanzi_count: # count values only if the other configs are false enable: true # custom uv span for the whole site site_uv: true site_uv_header: &lt;i class=&quot;fa fa-user&quot;&gt;&lt;/i&gt; site_uv_footer: # custom pv span for the whole site site_pv: true site_pv_header: &lt;i class=&quot;fa fa-eye&quot;&gt;&lt;/i&gt; site_pv_footer: # custom pv span for one page only page_pv: true page_pv_header: &lt;i class=&quot;fa fa-eye&quot;&gt;&lt;/i&gt; page_pv_footer: local_search: enable: true pace: trueplugins: hexo-generate-feed GitHub配置123456789101112$ git config --global user.name &quot;xxxx&quot;$ git config --global user.email &quot;xxxx@qq.com&quot;$ cd ~/.ssh$ lsid_rsa id_rsa.pub known_hosts$ ssh-keygen -t rsa -C &quot;xxxx@qq.com&quot; //三次直接回车，，生成密钥，最后得到了两个文件：id_rsa和id_rsa.pub（默认存储路径是：C:\Users\Administrator\.ssh）$ eval &quot;$(ssh-agent -s)&quot; //添加密钥到ssh-agent$ ssh-add ~/.ssh/id_rsa //添加生成的SSH key到ssh-agent登录Github，点击头像下的settings，添加ssh新建一个new ssh key，将id_rsa.pub文件里的内容复制上去,并验证$ ssh -T git@github.comHi air-project! You&apos;ve successfully authenticated, but GitHub does not provide shell access. Hexo 简单命令12345678重启$ hexo s删除$ hexo clean重新生成静态页面，并推送到GitHub$ hexo d -g]]></content>
      <categories>
        <category>hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
</search>
